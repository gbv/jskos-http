This example requires the [Slim micro framework for PHP](https://www.slimframework.com/).

    $ composer install
    $ php -S localhost:8000

The service always returns a dummy Concept echoing query parameters and path.
